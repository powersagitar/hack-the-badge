# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty (no source code, build tooling, or git history yet). This file is a placeholder — update it once the project takes shape.

When code is added, update this file to cover:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture and structure that isn't obvious from browsing individual files
